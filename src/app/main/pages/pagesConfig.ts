import Error404PageConfig from './errors/404/Error404PageConfig';
import Error500PageConfig from './errors/500/Error500PageConfig';
import TeamworksPageConfig from './teamworks/TeamworksPageConfig';
import UsersPageConfig from './users/UsersPageConfig';

const pagesConfigs = [Error404PageConfig, Error500PageConfig, TeamworksPageConfig, UsersPageConfig];

export default pagesConfigs;
